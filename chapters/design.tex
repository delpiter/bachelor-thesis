\chapter{Progettazione}
\label{chap:design}
In questo capitolo vengono illustrate e motivate le principali decisioni architetturali prese durante lo sviluppo del servizio.
Nello specifico, vengono descritte le scelte relative ai componenti che lo costituiscono, con particolare attenzione ai requisiti funzionali e non funzionali individuati nella fase di analisi.
Per alcune delle decisioni prese vengono inoltre discusse le alternative valutate e le motivazioni che hanno portato alla soluzione adottata, così da fornire una visione chiara e completa del percorso progettuale seguito.

\section{Gestione trasparente delle versioni IP}
\label{sec:ipversion-handler}
Il servizio di downstream deve essere in grado di gestire in maniera trasparente le due versioni del protocollo \ac{IP} (IPv4 e IPv6), evitando che la logica applicativa debba occuparsi esplicitamente delle differenze tra i due protocolli.
Le operazioni specifiche di ciascuna versione vengono quindi delegate a una classe dedicata che, sfruttando il pattern \emph{strategy} \cite[pp.~315--323]{gamma1994design} (\Cref{fig:icmp-stretegy}), incapsula le diverse implementazioni necessarie all'invio del messaggio \ac{ICMP} in base alla versione del protocollo \ac{IP} utilizzata.

Rispetto a una soluzione basata su costrutti condizionali (\texttt{if}/\texttt{else} o \texttt{switch}) sparsi nel codice, questa scelta consente di aggiungere in futuro il supporto a nuove versioni del protocollo, o nuove modalità di invio, senza dover modificare il codice client che utilizza tali classi, rispettando così il principio open/closed.
\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{figures/icmp-strategy.pdf}
    \caption[Diagramma delle classi del pattern strategy.]{La classe \texttt{EchoStream} delega il comportamento di apertura e chiusura del canale ad un oggetto \texttt{ICMPStrategy} intercambiabile.}
    \label{fig:icmp-stretegy}
\end{figure}


\section{Fan-out parallelo}
\label{sec:parallel-fanout}
L'upstream aggregator ha il compito di eseguire il fan-out\sidenote{Il fan-out si riferisce all'atto di distribuire le informazioni da una singola fonte verso più destinazioni.} verso gli host selezionati.
Poiché il numero di server da interrogare potrebbe aumentare notevolmente nel tempo, non è possibile eseguire le chiamate in sequenza: con un numero elevato di interrogazioni, l'esecuzione verrebbe inevitabilmente interrotta dal timeout imposto. È quindi necessario eseguire le chiamate in parallelo.
L'operazione è di tipo \emph{embarrassingly parallel} \cite{mattson2004patterns}, poiché ogni singola esecuzione è indipendente dalle altre e non richiede alcuna forma di sincronizzazione tra i vari task.

Tuttavia, senza alcuna limitazione, il sistema potrebbe consumare una quantità eccessiva di risorse al crescere del numero di server da interrogare, violando il vincolo introdotto nella \cref{sec:limitazioni}.
Per questo motivo è stato scelto di limitare il numero di unità di esecuzione concorrenti sfruttando il \emph{bounded parallelism pattern} attraverso un meccanismo di semafori e monitor, evitando il degrado della rete e un utilizzo eccessivo delle risorse macchina.
Sfruttando tale pattern, l'esecuzione del fan-out viene eseguita secondo il paradigma di programmazione parallela \emph{master-worker} come in figura \cref{fig:master-worker}, in cui un'unità coordinatrice (\emph{master}) distribuisce i task alle unità esecutrici (\emph{worker}), garantendo così il rispetto del vincolo di parallelismo imposto.
\begin{figure}
    \centering
    \includegraphics[width=.8\linewidth,trim=0 0 0 2cm]{figures/master-worker.pdf}
    \caption[Diagramma di sequenza dell'esecuzione del fan-out parallelo.]{Diagramma di sequenza di un esempio di esecuzione del fan-out parallelo con tre nodi worker.}
    \label{fig:master-worker}
\end{figure}
\section{Gestione dei middleware}
\label{sec:middleware-handler}
A seguito di una richiesta \ac{HTTP} potrebbe essere necessario eseguire più funzioni, ciascuna con uno scopo differente. Si vuole poter eseguire tali funzioni in sequenza, con la possibilità di interrompere l'esecuzione della catena in qualsiasi momento, ad esempio nel caso in cui una di esse rilevi una condizione di errore.

Per la gestione dei middleware in sequenza è stato adottato il design pattern \emph{chain of responsibility} \cite[pp.~223--232]{gamma1994design}(\Cref{fig:http-middleware}), che permette di far scorrere la richiesta lungo una catena di ``handler''. Ogni handler, dopo aver processato la richiesta, può decidere se passarla al successivo elemento della catena oppure se interrompere l'esecuzione, restituendo direttamente una risposta.

% Un esempio di flusso di informazione si può vedere in (ref a grafico)
Alcuni esempi di handler sono la gestione del logging strutturato e il disaster recovery. Sia il servizio di downstream sia quello di upstream utilizzano questa struttura, garantendo un comportamento uniforme e riutilizzabile lungo tutta l'architettura.
\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{figures/http-middleware.pdf}
    \caption[Diagramma delle classi del pattern chain of responsibility.]{Specifico nel caso del servizio downstream. Alla creazione dell'gestore della richiesta vengono aggiunti tutti i middleware necessari alla corretta esecuzione della richiesta. Il \texttt{IpWhitelistMiddleware} ha il compito di filtrare le richieste ricevute in base all'indirizzo del mittente.
    Questo middleware può terminare la catena di esecuzione.}
    \label{fig:http-middleware}
\end{figure}


\section{Caricamento delle variabili d'ambiente}
\label{sec:variable-load}
Sia il servizio di upstream che quello di downstream necessitano, in fase di avvio, del caricamento di un insieme di variabili d'ambiente indispensabili per il corretto funzionamento del sistema (ad esempio parametri di connessione, endpoint, credenziali o soglie operative).
Il design adottato in figura \cref{fig:config-loader} centralizza il caricamento e la validazione della configurazione delle singole componenti dell'applicazione a partire dalle variabile d'ambiente, restituendo un oggetto completamente validato oppure un errore esplicito al primo problema riscontrato.
\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{figures/config-loader.pdf}
    \caption[Caricatore delle configurazioni.]{Per il caricamento della configurazione vengono esposte una serie di classi, una per ciascun tipo di controllo da eseguire sulle varie variabili d'ambiente.}
    \label{fig:config-loader}
\end{figure}
Ogni tipo di dato ha una funzione di validazione dedicata che restituisce il valore o un errore. Questo separa la logica di validazione dalla logica di orchestrazione del caricamento, rendendo ogni singolo controllo testabile in isolamento.

\section{Integrazione con FlashStart 2026}
\label{sec:app-integration-design}
Si vuole permettere agli utenti di \emph{FlashStart 2026} di usufruire del servizio di latenza. È dunque necessario aggiungere un nuovo entrypoint all'interno dell'applicazione. Per mantenere una codebase coerente con il resto del sistema, sviluppato dagli altri developer dell'azienda, si è scelto di progettare il nuovo entrypoint seguendo il pattern M.V.C.

L'entrypoint è suddiviso in tre componenti distinti:
\begin{itemize}
    \item il \texttt{LatencyController}, elemento intermediario tra la view e il servizio vero e proprio;
    \item il \texttt{LatencyService}, contenente l'effettiva business logic e il controllo dei permessi;
    \item una serie di repository, che fungono da tramite tra il servizio e le fonti di informazione, quali un database o il servizio di upstream.
\end{itemize}

L'architettura risultante, illustrata in \cref{fig:fs-integration-mvc}, evidenzia questa suddivisione e la chiara separazione delle responsabilità tra i vari componenti, garantendo al sistema un'elevata semplicità di manutenzione ed estendibilità nel tempo.

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{figures/fs-integration-mvc.pdf}
    \caption[Architettura del backend per FlashStart 2026.]{Per esporre correttamente le informazioni richieste in fase di analisi, la classe \texttt{LatencyService} deve unire i risultati di due richieste differenti: la richiesta di latenza dall'upstream aggregator e i dati dei server dal database.}
    \label{fig:fs-integration-mvc}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=.9\linewidth]{figures/service-decorator.pdf}
    \caption[Diagramma delle classi del pattern decorator.]{Pattern utilizzato dai vari servizi all'interno della piattaforma FlashStart 2026, permette di eseguire autenticazione, autorizzazione e logging strutturato senza modificare il codice interno del servizio, mantenendo separate le diverse logiche.}
    \label{fig:service-decorator}
\end{figure}
Al fine di mantenere separata la logica applicativa dalla gestione di autenticazione e autorizzazione, questi ultimi vengono controllati tramite il pattern \emph{decorator} \cite[pp.~175--184]{gamma1994design} come illustrato in figura \cref{fig:service-decorator}, sfruttando le strutture già presenti nella codebase dell'applicazione.